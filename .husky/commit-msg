# commitlint is not installed, skipping commit message validation
# To enable commitlint, install it: pnpm add -D commitlint @commitlint/config-conventional
# and create a commitlint.config.js file
echo "⚠️  commitlint is not installed. Skipping commit message validation."
